@page
@model SalonASP.Pages.Clientis.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Lista clienti</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Prenume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Telefon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clienti[0].Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Clienti) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ClientiID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ClientiID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ClientiID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
